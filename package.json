{
  "name": "mindscape-explorer",
  "version": "1.0.0",
  "description": "Consciousness exploration through media",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p public && cp *.html public/ && cp *.css public/ && cp *.js public/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}